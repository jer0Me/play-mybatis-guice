<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="models.daos.UsersDao">

    <select id="getUsers" flushCache="true" resultType="models.dtos.UserDto">

        SELECT id, name, age, address
        FROM ex01_schema.tm_user

    </select>

</mapper>